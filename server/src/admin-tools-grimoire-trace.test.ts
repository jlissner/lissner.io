import { describe, it } from "vitest";

describe("Gherkin: Admin tools beyond duplicate detection (features/admin/admin-tools.feature)", () => {
  it.todo(
    "Scenario: View and edit email whitelist — see server/src/routes/admin/whitelist-routes.ts",
  );

  it.todo(
    "Scenario: SQL explorer when enabled — see server/src/routes/admin/sql-routes.ts, feature-gates",
  );

  it.todo(
    "Scenario: Repair thumbnails — see server/src/routes/admin/thumbnails-routes.test.ts, thumbnails-routes.ts",
  );

  it.todo(
    "Scenario: List database backups — see server/src/routes/admin/db-backup-routes.ts",
  );

  it.todo(
    "Scenario: Restore database from a backup — see server/src/routes/admin/db-backup-routes.ts, server/src/s3/startup-db-restore.test.ts",
  );

  it.todo(
    "Scenario: Data explorer browse tables — see server/src/routes/admin/data-explorer-routes.ts",
  );
});
